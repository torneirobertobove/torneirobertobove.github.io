name: Deploy GitHub Pages (DISABLED)

on:
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy Pages workflow disabled. Use Admin Wizard Fix Deploy."
