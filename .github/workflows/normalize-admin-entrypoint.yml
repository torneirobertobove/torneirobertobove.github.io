name: Normalize admin entrypoint

# DISABLED: legacy workflow rewrote admin.html and conflicted with the native runtime.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy admin entrypoint normalizer disabled."
