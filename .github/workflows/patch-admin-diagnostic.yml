name: Patch admin wizard diagnostic compatibility

# DISABLED: diagnostic compatibility must not rewrite the authoritative wizard.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "One-time diagnostic patch workflow disabled."
