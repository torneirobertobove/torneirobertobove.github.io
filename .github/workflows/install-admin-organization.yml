name: Install admin organization

# DISABLED: legacy self-mutating workflow. Native admin.html is authoritative.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy admin organization installer disabled."
