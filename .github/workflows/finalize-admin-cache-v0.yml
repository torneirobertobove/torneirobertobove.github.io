name: Finalize admin cache warnings

# DISABLED: legacy cache normalization must not rewrite admin.html.
# Trigger marker: admin wizard V20 deployment verification.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy admin cache normalization is disabled."
