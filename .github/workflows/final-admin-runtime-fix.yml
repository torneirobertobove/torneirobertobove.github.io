name: Final Admin Runtime Fix

# DISABLED: admin.html is now maintained directly.
# The old workflow rewrote admin.html on every push and could leave the page in a
# permanently loading/broken state. Keep this file only as a historical marker.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Final Admin Runtime Fix is disabled."
