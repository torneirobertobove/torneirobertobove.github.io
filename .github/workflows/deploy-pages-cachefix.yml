name: Deploy GitHub Pages Cache Fix (DISABLED)

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy cache-fix Pages workflow disabled. Use Admin Wizard Fix Deploy."
