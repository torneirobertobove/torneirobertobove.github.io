name: Bove repair and validation

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  repair-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Repair Bove display cleanly
        shell: python
        run: |
          from pathlib import Path
          import re

          p = Path('Bove.html')
          text = p.read_text(encoding='utf-8')

          text = re.sub(
              r'function normalizzaCognomiGironi\(\)\s*\{.*?\n\}\s*',
              '',
              text,
              count=1,
              flags=re.S
          )
          text = re.sub(
              r'function soloCognome\(valore\)\s*\{.*?\n\}\s*',
              '',
              text,
              count=1,
              flags=re.S
          )

          text, n = re.subn(
              r'\n\s*/\*\s*=+\s*\n\s*RITORNO\s*\n\s*=+\s*\n\s*\*/\s*\n\s*let modal\s*=.*?\n\s*if\(modal\).*?\n\s*modal\.style\.display="none";\s*\n\s*\}\s*\n\s*function loadSavedTeams\(\)',
              '\n\nfunction loadSavedTeams()',
              text,
              count=1,
              flags=re.S
          )
          if n:
              print('PATCH APPLICATA: rimossa la chiusura duplicata di applyRules()')

          render_class = r'''function renderClass(){

    ensureRules();

    const gironi = ["A","B","C","D","E","F","G"];

    function soloCognomiClassifica(valore){

        if(valore === null || valore === undefined)
            return "";

        if(typeof valore === "object"){

            if(valore.cognome !== undefined && valore.cognome !== null){
                const c = String(valore.cognome).trim();
                if(c) return c;
            }

            const g1 = valore.giocatore1 || valore.g1 || valore.player1 || null;
            const g2 = valore.giocatore2 || valore.g2 || valore.player2 || null;

            const c1 = soloCognomiClassifica(g1);
            const c2 = soloCognomiClassifica(g2);

            if(c1 && c2) return c1 + "/" + c2;
            return c1 || c2 || "";
        }

        const testo = String(valore)
            .replace(/\t/g," ")
            .replace(/\s+/g," ")
            .trim();

        if(!testo) return "";

        return testo
            .split("/")
            .map(parte => {
                const parole = parte.trim().split(/\s+/).filter(Boolean);
                return parole.length ? parole[parole.length - 1] : "";
            })
            .filter(Boolean)
            .join("/");
    }

    gironi.forEach(tag => {

        const tbody = document.getElementById(tag + "class");
        if(!tbody) return;

        tbody.innerHTML = "";

        const squadre = Array.isArray(state[tag]) ? state[tag] : [];
        if(squadre.length === 0) return;

        const risultati = Array.isArray(state[tag + "res"])
            ? state[tag + "res"]
            : [];

        const classifica = calc(squadre, risultati);
        if(!Array.isArray(classifica) || classifica.length === 0)
            return;

        classifica.forEach((x, indice) => {

            const tr = document.createElement("tr");

            const tdPos = document.createElement("td");
            tdPos.textContent = String(indice + 1);

            const tdNome = document.createElement("td");
            tdNome.textContent = soloCognomiClassifica(x?.n ?? "");

            const tdFreccia = document.createElement("td");
            tdFreccia.style.width = "24px";
            tdFreccia.style.textAlign = "center";
            tdFreccia.style.fontWeight = "700";
            tdFreccia.textContent = x?.qual ? "↑" : "↓";
            tdFreccia.style.color = x?.qual ? "#22c55e" : "#ef4444";

            const tdPt = document.createElement("td");
            tdPt.textContent = String(Number(x?.pt) || 0);

            const tdGf = document.createElement("td");
            tdGf.textContent = String(Number(x?.gf) || 0);

            const tdGs = document.createElement("td");
            tdGs.textContent = String(Number(x?.gs) || 0);

            const tdDf = document.createElement("td");
            tdDf.textContent = String(Number(x?.df) || 0);

            tr.appendChild(tdPos);
            tr.appendChild(tdNome);
            tr.appendChild(tdFreccia);
            tr.appendChild(tdPt);
            tr.appendChild(tdGf);
            tr.appendChild(tdGs);
            tr.appendChild(tdDf);

            tbody.appendChild(tr);
        });
    });

    console.log("RENDERCLASS — SOLO COGNOMI", {
        A: document.getElementById("Aclass")?.innerText || "",
        B: document.getElementById("Bclass")?.innerText || ""
    });
}
'''

          pattern = re.compile(
              r'function renderClass\(\)\s*\{.*?\n\}\s*\n\s*function updateKOResult\(',
              re.S
          )
          m = pattern.search(text)
          if not m:
              raise SystemExit('Blocco renderClass non trovato')

          text = text[:m.start()] + render_class + '\nfunction updateKOResult(' + text[m.end():]

          p.write_text(text, encoding='utf-8')
          print('PATCH APPLICATA: helper temporanei rimossi e renderClass corretto')

      - name: Repair getFinalQualified function structure
        shell: python
        run: |
          from pathlib import Path
          import re

          p = Path('Bove.html')
          text = p.read_text(encoding='utf-8')

          # Caso corrotto: il corpo di getFinalQualified() è presente,
          # ma la riga function getFinalQualified(){ è stata cancellata.
          if 'function getFinalQualified()' not in text:
              start = re.search(r'\n\s*ensureRules\(\);\s*\n\s*const qualificati\s*=\s*\[\];', text)
              end = re.search(r'\n\s*function nomeGiocatore\s*\(', text)

              if start and end and start.start() < end.start():
                  body_start = start.start()
                  body = text[body_start:end.start()]
                  replacement = '\n\nfunction getFinalQualified(){' + body + '\n}\n\n'
                  text = text[:body_start] + replacement + text[end.start():]
                  print('PATCH APPLICATA: ripristinata function getFinalQualified()')
              else:
                  raise SystemExit('Impossibile individuare il corpo corrotto di getFinalQualified()')
          else:
              print('getFinalQualified() già presente: nessuna modifica')

          p.write_text(text, encoding='utf-8')

      - name: Fix mobile gironi layout
        shell: python
        run: |
          from pathlib import Path
          import re

          p = Path('Bove.html')
          text = p.read_text(encoding='utf-8')

          mobile = r'''@media(max-width:599px){
    body{overflow-x:hidden;}
    .groups{display:block;width:100%;min-width:0!important;}
    .groups .card{width:100%!important;max-width:100%!important;overflow-x:hidden!important;padding:8px;}
    .groups .card table{width:100%!important;min-width:0!important;max-width:100%!important;table-layout:fixed!important;}
    .groups .card table th,
    .groups .card table td{min-width:0!important;}
    .groups .card table:first-of-type th:nth-child(1),
    .groups .card table:first-of-type td:nth-child(1){width:48px!important;}
    .groups .card table:first-of-type th:nth-child(2),
    .groups .card table:first-of-type td:nth-child(2){width:58px!important;}
    .groups .card table:first-of-type th:nth-child(4),
    .groups .card table:first-of-type td:nth-child(4){width:72px!important;}
    .groups .card table:first-of-type th:nth-child(3),
    .groups .card table:first-of-type td:nth-child(3){width:auto!important;}
    .groups .card table:first-of-type .team-line{font-size:12px;white-space:normal!important;overflow:visible!important;text-overflow:clip!important;word-break:break-word;}
    .groups .card table:first-of-type .match-cell{white-space:normal!important;overflow:visible!important;}
    .groups .card table:first-of-type .db-cell{gap:2px;}
    .groups .card table:first-of-type .db-cell input{width:28px!important;min-width:28px!important;max-width:28px!important;height:30px!important;}
    .groups .card table:nth-of-type(2){font-size:11px;}
    .groups .card table:nth-of-type(2) th:first-child,
    .groups .card table:nth-of-type(2) td:first-child{width:110px!important;max-width:110px!important;white-space:normal!important;}
}
'''

          text = re.sub(
              r'\n@media\(max-width:599px\)\{\s*/\* BOVE MOBILE GIRONI FIX START \*/.*?/\* BOVE MOBILE GIRONI FIX END \*/\s*\}\s*',
              '\n',
              text,
              count=1,
              flags=re.S
          )

          marker = '</style>'
          if marker not in text:
              raise SystemExit('Chiusura </style> non trovata')

          wrapped = '\n/* BOVE MOBILE GIRONI FIX START */\n' + mobile + '/* BOVE MOBILE GIRONI FIX END */\n'
          text = text.replace(marker, wrapped + marker, 1)

          p.write_text(text, encoding='utf-8')
          print('PATCH APPLICATA: gironi mobile senza scroll orizzontale')

      - name: JavaScript syntax test
        shell: python
        run: |
          from pathlib import Path
          import re, subprocess, tempfile

          html = Path('Bove.html').read_text(encoding='utf-8')
          scripts = re.findall(r'<script(?:\s[^>]*)?>(.*?)</script>', html, re.S | re.I)
          if not scripts:
              raise SystemExit('Nessun blocco script trovato')

          for i, script in enumerate(scripts, 1):
              f = Path(tempfile.gettempdir()) / f'bove_script_{i}.js'
              f.write_text(script, encoding='utf-8')
              r = subprocess.run(['node', '--check', str(f)], text=True, capture_output=True)
              if r.returncode != 0:
                  print(r.stdout)
                  print(r.stderr)
                  raise SystemExit(f'Sintassi JavaScript non valida nello script {i}')

          print(f'SINTASSI OK: {len(scripts)} blocchi script')

      - name: Structural validation
        shell: python
        run: |
          from pathlib import Path
          import re

          text = Path('Bove.html').read_text(encoding='utf-8')

          required = [
              'function loadState()',
              'function initDefaults()',
              'function renderTables()',
              'function renderClass()',
              'function updateCamp(',
              'function updateRes(',
              'function getFinalQualified()',
              'function creaFinale(semifinali)',
              'function toggleAdminMenu()',
              'function verifyAccess()',
              'id="adminMenuButton"',
              'id="adminMenu"',
              'id="titoloTorneo"',
              'id="nomeTorneoInput"',
              'type="date"'
          ]

          missing = [x for x in required if x not in text]
          if missing:
              raise SystemExit('Elementi mancanti: ' + ', '.join(missing))

          pos_get = text.find('function getFinalQualified()')
          pos_finale = text.find('function creaFinale(semifinali)')
          if pos_get == -1 or pos_finale == -1 or pos_get >= pos_finale:
              raise SystemExit('Ordine fase finale non valido: getFinalQualified() deve precedere creaFinale(semifinali)')

          for ident in ['teamModal', 'adminMenuButton', 'adminMenu']:
              n = len(re.findall(r'id=["\']' + re.escape(ident) + r'["\']', text))
              if n != 1:
                  raise SystemExit(f'ID duplicato/assente: {ident} ({n})')

          if 'function normalizzaCognomiGironi()' in text:
              raise SystemExit('Helper temporaneo normalizzaCognomiGironi ancora presente')

          if 'function soloCognome(valore)' in text:
              raise SystemExit('Helper temporaneo soloCognome ancora presente')

          print('VALIDAZIONE STRUTTURA OK — getFinalQualified() presente prima di creaFinale()')

      - name: Commit repair
        shell: bash
        run: |
          if git diff --quiet -- Bove.html; then
            echo 'Nessuna modifica necessaria.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add Bove.html
          git commit -m 'Repair getFinalQualified syntax and validate Bove'
          git push
