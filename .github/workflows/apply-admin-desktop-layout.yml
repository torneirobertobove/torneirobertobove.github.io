name: Clean Admin Desktop Override

# DISABLED: this legacy self-mutating workflow rewrote the live admin shell
# and downgraded the runtime scripts. The native admin files are authoritative.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy Admin Desktop Override disabled."