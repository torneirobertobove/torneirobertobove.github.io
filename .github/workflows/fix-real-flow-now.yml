name: Fix real tournament flow now

# DISABLED: this one-time repair workflow is no longer allowed to rewrite the live admin flow.
# The authoritative deployment is admin-wizard-fix-deploy.yml.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "One-time real-flow repair disabled."
