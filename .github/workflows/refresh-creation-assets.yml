name: Refresh creation assets

# DISABLED: cache-busting workflow is no longer allowed to rewrite admin.html.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy creation asset refresher disabled."