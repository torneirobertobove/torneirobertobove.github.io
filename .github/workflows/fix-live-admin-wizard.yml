name: Fix live admin wizard version

# DISABLED: this historical one-time workflow could downgrade the wizard reference.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Historical live-admin wizard fix disabled."
