name: Force admin cache normalization

# DISABLED: this legacy workflow re-injected admin-final-click-fix-v1.js
# and rewrote admin.html, which can break the native admin runtime.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy admin cache normalization is disabled."
