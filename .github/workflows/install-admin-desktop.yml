name: Disable legacy admin desktop injector

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy admin desktop injection disabled; admin.html owns the approved dashboard layout."
