name: Install approved admin desktop shell

# DISABLED: legacy self-mutating workflow. Native admin.html is authoritative.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy admin desktop installer disabled."
