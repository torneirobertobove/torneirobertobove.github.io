/*
 * Legacy desktop overlay intentionally disabled.
 * The current admin.html contains the authoritative new dashboard layout.
 * Functional logic remains in admin-functions.js.
 */
(() => {
  'use strict';
  // No DOM replacement. Keep this file as a harmless compatibility shim
  // because older admin.html revisions may still reference it.
})();
