/* ADMIN COMUNICAZIONI V1 — aggiunta isolata a News / Sponsor / WhatsApp.
   Non modifica schema Supabase: salva i dati in tornei.configurazione.
*/
(()=>{
'use strict';
/* Un solo comando "Nuovo torneo": admin.html ne fornisce già uno nella topbar. */
const style=document.createElement('style');
style.id='admin-single-new-tournament';
style.textContent='#appContent #newTournament{display:none !important;}';
(document.head||document.documentElement).appendChild(style);
const $=id=>document.getElementById(id);
const state=()=>window.adminState||{};
const selected=()=>window.getTorneoAdminCorrente?.()||((state().tornei||[]).find(t=>String(t.id)===String(state().torneoSelezionato))||null);
const esc=v=>String(v??'').replace(/[&<>\"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[m]));
const cfgOf=t=>t?.configurazione&&typeof t.configurazione==='object'?{...t.configurazione}:{};
async function saveCfg(t,cfg){const sb=window.supabaseClient||window.sb;if(!sb||!t){alert('Torneo o connessione Supabase non disponibile.');return false}const{error}=await sb.from('tornei').update({configurazione:cfg}).eq('id',t.id);if(error){console.error(error);alert('Errore salvataggio: '+error.message);return false}t.configurazione=cfg;window.adminState=state();try{localStorage.setItem('padel_admin_state',JSON.stringify(state()))}catch(e){}return true}
function shell(title,sub,body){const root=$('appContent');if(!root)return;root.innerHTML=`<div class="page-head"><div><h1>${title}</h1><p>${sub}</p></div><button class="btn" id="comBack">← Torna al torneo</button></div>${body}`;$('comBack')?.addEventListener('click',()=>window.openAdminPage?.('torneo'))}
function news(){const t=selected();if(!t){alert('Seleziona prima un torneo');return}const c=cfgOf(t),items=Array.isArray(c.news)?c.news:[];shell('News',`${esc(t.nome)} · ID ${esc(t.id)}`,`<div class="card"><div class="card-head"><h2>Gestione News</h2></div><div class="card-body"><div class="section-grid"><div><label>Titolo</label><input id="newsTitle" class="input" placeholder="Titolo della news"><label>Testo</label><textarea id="newsText" class="input" rows="6" placeholder="Testo della comunicazione"></textarea><button class="btn primary" id="newsSave">＋ Pubblica news</button></div><div><h3>News del torneo</h3><div id="newsList">${items.length?items.map((n,i)=>`<div class="list-item"><strong>${esc(n.titolo)}</strong><small>${esc(n.testo)}</small><button class="btn small danger" data-news-del="${i}">Elimina</button></div>`).join(''):'<div class="empty">Nessuna news pubblicata.</div>'}</div></div></div></div></div>`);$('newsSave').onclick=async()=>{const titolo=$('newsTitle')?.value.trim(),testo=$('newsText')?.value.trim();if(!titolo||!testo){alert('Inserisci titolo e testo della news.');return}const next=[...items,{id:'news-'+Date.now(),titolo,testo,data:new Date().toISOString()}];if(await saveCfg(t,{...c,news:next}))news()};document.querySelectorAll('[data-news-del]').forEach(b=>b.onclick=async()=>{const next=items.filter((_,i)=>i!==Number(b.dataset.newsDel));if(await saveCfg(t,{...c,news:next}))news()})}
function sponsor(){const t=selected();if(!t){alert('Seleziona prima un torneo');return}const c=cfgOf(t),items=Array.isArray(c.sponsor)?c.sponsor:[];shell('Sponsor',`${esc(t.nome)} · ID ${esc(t.id)}`,`<div class="card"><div class="card-head"><h2>Gestione Sponsor</h2></div><div class="card-body"><div class="section-grid"><div><label>Nome sponsor</label><input id="sponsorName" class="input" placeholder="Nome sponsor"><label>Link sponsor</label><input id="sponsorUrl" class="input" placeholder="https://..."><label>Descrizione</label><textarea id="sponsorText" class="input" rows="4" placeholder="Descrizione o messaggio"></textarea><button class="btn primary" id="sponsorSave">＋ Aggiungi sponsor</button></div><div><h3>Sponsor del torneo</h3><div id="sponsorList">${items.length?items.map((n,i)=>`<div class="list-item"><strong>${esc(n.nome)}</strong><small>${esc(n.descrizione||'')}${n.url?' · '+esc(n.url):''}</small><button class="btn small danger" data-sponsor-del="${i}">Elimina</button></div>`).join(''):'<div class="empty">Nessuno sponsor configurato.</div>'}</div></div></div></div></div>`);$('sponsorSave').onclick=async()=>{const nome=$('sponsorName')?.value.trim(),url=$('sponsorUrl')?.value.trim(),descrizione=$('sponsorText')?.value.trim();if(!nome){alert('Inserisci il nome dello sponsor.');return}const next=[...items,{id:'sponsor-'+Date.now(),nome,url,descrizione}];if(await saveCfg(t,{...c,sponsor:next}))sponsor()};document.querySelectorAll('[data-sponsor-del]').forEach(b=>b.onclick=async()=>{const next=items.filter((_,i)=>i!==Number(b.dataset.sponsorDel));if(await saveCfg(t,{...c,sponsor:next}))sponsor()})}
function whatsapp(){const t=selected();if(!t){alert('Seleziona prima un torneo');return}const link=location.origin+'/Bove.html?idTorneo='+encodeURIComponent(t.id);shell('WhatsApp',`${esc(t.nome)} · ID ${esc(t.id)}`,`<div class="card"><div class="card-head"><h2>Comunicazioni WhatsApp</h2></div><div class="card-body"><label>Messaggio</label><textarea id="waText" class="input" rows="6">Ciao! Ti invitiamo al torneo ${esc(t.nome)} del ${esc(t.data||t.data_torneo||'')}.\n\n${esc(link)}</textarea><div class="admin-feature-actions"><button class="btn primary" id="waOpen">📱 Apri WhatsApp</button><button class="btn" id="waCopy">📋 Copia link torneo</button></div><div class="notice" style="margin-top:14px">Il link è sempre riferito al torneo attualmente selezionato.</div></div></div>`);$('waOpen').onclick=()=>window.open('https://wa.me/?text='+encodeURIComponent($('waText')?.value||''),'_blank');$('waCopy').onclick=()=>navigator.clipboard?.writeText(link).then(()=>alert('Link copiato negli appunti.'))}
function addNav(){const sidebar=document.querySelector('#areaAdmin .sidebar .nav-group');if(sidebar&&!document.getElementById('adminComNav')){const g=document.createElement('div');g.className='nav-group';g.id='adminComNav';g.innerHTML='<div class="nav-label">Comunicazioni</div><nav class="nav"><button data-com-page="news">📰 News</button><button data-com-page="sponsor">🤝 Sponsor</button><button data-com-page="whatsapp">📱 WhatsApp</button></nav>';sidebar.parentElement.insertBefore(g,sidebar.parentElement.querySelector('.sidebar-footer'))}const mobile=document.querySelector('#mobileOverlay .mobile-nav');if(mobile&&!document.getElementById('mobileComNav')){const wrap=document.createElement('div');wrap.id='mobileComNav';wrap.innerHTML='<button data-com-page="news">📰 News</button><button data-com-page="sponsor">🤝 Sponsor</button><button data-com-page="whatsapp">📱 WhatsApp</button>';mobile.appendChild(wrap)}document.querySelectorAll('[data-com-page]').forEach(b=>{if(b.dataset.bound)return;b.dataset.bound='1';b.addEventListener('click',()=>{document.getElementById('mobileOverlay')?.classList.remove('open');const p=b.dataset.comPage;p==='news'?news():p==='sponsor'?sponsor():whatsapp()})})}
/* COLLEGAMENTO SIDEBAR REALE: i menu laterali aprono la pagina/funzione associata. */
function bindSidebar(){
  document.querySelectorAll('#areaAdmin .sidebar [data-page]').forEach(b=>{
    if(b.dataset.sidebarBound)return;
    b.dataset.sidebarBound='1';
    b.addEventListener('click',async()=>{
      const page=b.dataset.page;
      if(!page)return;
      document.querySelectorAll('#areaAdmin .sidebar [data-page]').forEach(x=>x.classList.remove('active'));
      b.classList.add('active');
      if(typeof window.openAdminPage==='function') await window.openAdminPage(page);
    });
  });
}
function bindAll(){addNav();bindSidebar();setTimeout(()=>{addNav();bindSidebar()},200)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bindAll,{once:true});else bindAll();
window.openAdminComPage=p=>p==='news'?news():p==='sponsor'?sponsor():whatsapp();
})();
